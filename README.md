This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

You can start editing the page by modifying `app/page.tsx`. The page auto-updates as you edit the file.

This project uses [`next/font`](https://nextjs.org/docs/app/building-your-application/optimizing/fonts) to automatically optimize and load [Geist](https://vercel.com/font), a new font family for Vercel.

## Folder Structure
Eco-Landing-Page/
├── .next/                  # (auto-generated by Next.js on build)
├── node_modules/           # Dependencies
├── public/                 # Static assets (images, fonts, favicons, etc.)
│   ├── images/
│   └── favicon.ico
├── src/                    # (recommended: keep your source code here)
│   ├── app/                # App Router (pages, layouts, routes)
│   │   ├── layout.tsx      # Root layout (applies to all pages)
│   │   ├── page.tsx        # Home page
│   │   ├── about/
│   │   │   └── page.tsx    # Example route (/about)
│   │   ├── dashboard/
│   │   │   ├── layout.tsx  # Layout for dashboard section
│   │   │   ├── page.tsx    # Dashboard index
│   │   │   └── settings/
│   │   │       └── page.tsx # Nested route (/dashboard/settings)
│   │   └── api/            # Next.js API routes (serverless functions)
│   │       └── hello/route.ts
│   │
│   ├── components/         # Shared/reusable UI components
│   │   ├── ui/             # Low-level UI components (buttons, modals)
│   │   ├── layout/         # Layout-related components (headers, footers, navbar)
│   │   └── widgets/        # Reusable sections (cards, charts, forms)
│   │
│   ├── hooks/              # Custom React hooks
│   │   └── useAuth.ts
│   │
│   ├── lib/                # Utilities & helpers (non-React functions)
│   │   ├── auth.ts
│   │   └── fetcher.ts
│   │
│   ├── services/           # External API calls, data-fetching logic
│   │   └── user.service.ts
│   │
│   ├── types/              # TypeScript types & interfaces
│   │   └── user.ts
│   │
│   ├── styles/             # Global styles (CSS, SCSS, Tailwind)
│   │   └── globals.css
│   │
│   ├── context/            # React Context providers
│   │   └── ThemeContext.tsx
│   │
│   ├── config/             # App-level configurations (env, constants)
│   │   └── site.config.ts
│   │
│   └── utils/              # General utility functions
│       └── formatter.ts
│
├── .env.local              # Local environment variables
├── .gitignore
├── next.config.js          # Next.js configuration
├── package.json
├── tsconfig.json           # TypeScript config
└── tailwind.config.js      # Tailwind CSS config (if using)


## Deploy on Vercel

The easiest way to deploy your Next.js app is to use the [Vercel Platform](https://vercel.com/new?utm_medium=default-template&filter=next.js&utm_source=create-next-app&utm_campaign=create-next-app-readme) from the creators of Next.js.

Check out our [Next.js deployment documentation](https://nextjs.org/docs/app/building-your-application/deploying) for more details.
